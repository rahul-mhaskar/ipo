import React from "react";

const BrokerPopup = ({ onClose, showMessage }) => {
  const brokers = [
    {
      name: "Zerodha",
      href: "https://zerodha.com/open-account?c=VCB643",
      logo: "https://zerodha.com/static/images/logo.svg",
    },
    {
      name: "Upstox",
      href: "https://upstox.onelink.me/0H1s/4LAYGW",
      logo: "https://assets.upstox.com/website/images/upstox-new-logo.svg",
    },
    {
      name: "Paytm Money",
      href: "https://paytmmoney.page.link/DSwSvdhoasovQYLz9",
      logo: "https://play-lh.googleusercontent.com/nXCY9Did341stoQEhCEH5wJW2FBybZYbpiYl2J-eCajYOXZ_XXXHX1ptjATuA0zayg",
    },
  ];

  return (
    <div className="fixed inset-0 bg-black bg-opacity-40 backdrop-blur-sm z-50 flex justify-center items-center">
      <div className="bg-white w-full max-w-md mx-4 p-6 rounded-xl shadow-lg relative">
        <button
          onClick={() => onClose(false)}
          className="absolute top-2 right-3 text-gray-500 hover:text-red-500"
        >
          &times;
        </button>
        <h2 className="text-xl font-bold text-center text-blue-700 mb-4">
          Open Demat Account with Top Brokers
        </h2>
        <div className="grid grid-cols-2 gap-4">
          {brokers.map((broker, idx) => (
            <a
              key={idx}
              href={broker.href}
              target="_blank"
              rel="noopener noreferrer"
              onClick={() => showMessage(`Redirecting to ${broker.name}`)}
              className="flex flex-col items-center p-3 border rounded-lg shadow-sm hover:shadow-md hover:scale-105 transition-all bg-gray-50"
            >
              <img src={broker.logo} alt={broker.name} className="h-10 object-contain mb-2" />
              <span className="text-sm font-medium text-gray-700">{broker.name}</span>
            </a>
          ))}
        </div>
      </div>
    </div>
  );
};

export default BrokerPopup;
